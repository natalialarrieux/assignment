public class AccountProcessor {
    @future public static void countContacts(List<Id> accountIds){
      //  List<Account> accounts = [Select Id, Name, Select Count(Id) from Contacts as CantContactos
    //                              from Account Where Id IN : accountIds];
  //    //  List<Contact> contacts = [Select Id from Contact Where Id IN : contacts];
        
    }
}